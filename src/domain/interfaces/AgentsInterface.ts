export interface FieldAgentInterface {
    name: string,
    opCode: string,
    email: string,
    leaderEmail: string,
    appVersion: string,
    device: string
}

export interface PhoneAgentInterface {
    name: string;
    opCode: string;
    email: string;
    leaderEmail: string;
}